FROM alpine:latest

RUN apk add build-base cmake make sudo

RUN addgroup sudo
RUN adduser -s /bin/ash -G sudo -D user
RUN echo '%sudo ALL=(ALL) NOPASSWD:ALL' >> /etc/sudoers

USER user

RUN sudo whoami
